{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User Profile",
      "type": "object",
      "description": "Represents the publicly visible and privately owned data for a user in the application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "The user's unique ID (matches Firebase Auth UID)."
        },
        "email": {
          "type": "string",
          "description": "User's email address. Should only be visible to the user themselves.",
          "format": "email"
        },
        "username": {
          "type": "string",
          "description": "The public username for the user."
        },
        "userCode": {
          "type": "string",
          "description": "A unique, shareable code for users to find each other (e.g., username#1234)."
        },
        "fullName": {
          "type": "string",
          "description": "The user's full name."
        },
        "avatarUrl": {
          "type": "string",
          "description": "URL of the user's avatar image.",
          "format": "uri"
        },
        "online": {
            "type": "boolean",
            "description": "Indicates if the user is currently online."
        },
        "lastSeen": {
            "type": "string",
            "description": "Timestamp of when the user was last active.",
            "format": "date-time"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the user account was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the user account was last updated.",
          "format": "date-time"
        }
      },
      "required": ["id", "email", "username", "userCode", "fullName", "createdAt", "updatedAt"]
    },
    "Channel": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Channel",
      "type": "object",
      "description": "Represents a communication channel, which can be a public group, a private group, or a direct message (DM).",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the channel."
        },
        "name": {
          "type": "string",
          "description": "Display name of the channel."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the channel's purpose."
        },
        "isPublic": {
          "type": "boolean",
          "description": "If true, the channel is discoverable. If false, it's private."
        },
        "isDM": {
            "type": "boolean",
            "description": "If true, this channel is a direct message between two or more users."
        },
        "ownerId": {
          "type": "string",
          "description": "The UID of the user who owns and manages the channel."
        },
        "members": {
            "type": "array",
            "items": { "type": "string" },
            "description": "An array of user UIDs who are members of this channel. Used for security rules."
        },
        "automations": {
            "type": "array",
            "items": { "type": "object" },
            "description": "Configuration for any AI automations for this channel."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["id", "name", "ownerId", "members", "isPublic", "isDM", "createdAt", "updatedAt"]
    },
    "Message": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Message",
      "type": "object",
      "description": "A single message within a channel.",
      "properties": {
        "id": { "type": "string" },
        "authorId": {
          "type": "string",
          "description": "The UID of the user who sent the message."
        },
        "content": {
          "type": "string",
          "description": "The text content of the message."
        },
        "timestamp": {
          "type": "string",
          "description": "The server timestamp when the message was sent.",
          "format": "date-time"
        },
        "readStatus": {
            "type": ["string", "null"],
            "enum": ["read", null],
            "description": "Indicates if the message has been read by recipients."
        }
      },
      "required": ["id", "authorId", "content", "timestamp"]
    }
  },
  "auth": {
    "providers": ["password", "google.com", "github.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/UserProfile" },
      "description": "Stores all user profile data. Each user's document is protected, and the collection can only be queried on indexed fields like 'userCode'."
    },
    "/channels/{channelId}": {
      "schema": { "$ref": "#/entities/Channel" },
      "description": "Stores data for all channels. Access is controlled by the `members` array in each document."
    },
    "/channels/{channelId}/messages/{messageId}": {
      "schema": { "$ref": "#/entities/Message" },
      "description": "Stores all messages for a given channel. Access permissions are inherited from the parent channel."
    }
  }
}
